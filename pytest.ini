[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
addopts =
    --cov=app
    --cov-report=term-missing
env =
    APP_MODE=paper
    DATABASE_DSN=postgresql+asyncpg://local:local@localhost:1145/aegis
